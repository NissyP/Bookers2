<h2>Book detail</h2>

<div class="post-body">
  <p>タイトル：<%= @book.title %></p>
  <p>説明：<%= @book.body %></p>
  <p>ユーザーネーム：<%= @book.user.name %></p>
  <% if @book.user == current_user %>
    <%= link_to "Destroy", book_path(@book), method: :delete %>
  <% end %>
</div>